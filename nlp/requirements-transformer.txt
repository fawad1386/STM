# Extra deps for the transformer model (training + inference).
# Install on top of requirements.txt:  pip install -r requirements-transformer.txt
torch>=2.0.0
transformers>=4.35.0
pandas>=2.0.0
scikit-learn>=1.3.0
